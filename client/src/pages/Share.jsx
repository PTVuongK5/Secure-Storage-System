import React from 'react'
import { FileStack, Share2 } from 'lucide-react'

export default function Share(){
  return (
    <div>
      <h2>Share (prototype)</h2>
      <p>Use this page to call POST /api/v1/files/{file_id}/share with recipient and role.</p>
    </div>
  )
}